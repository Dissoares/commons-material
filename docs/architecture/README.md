# Architecture

Documente aqui:
- contexto da biblioteca;
- objetivos de reuso entre empresas;
- fronteiras entre `components`, `core`, `theme` e `tokens`;
- regras de dependencia e anti-corruption layers.
